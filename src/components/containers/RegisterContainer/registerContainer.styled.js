import styled from 'styled-components';
import { GRAY_SCALE } from '@styles';

const Contain = styled.div`
  border: 2px solid ${GRAY_SCALE[100]};
  margin: 1rem 6rem 2rem;
  padding: 3rem 5rem;
`;

export default {
  Contain
};
